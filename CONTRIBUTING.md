# Contributing to pysl

## Installing pysl for development

1. Fork `lingpy/pysl`
2. Clone your fork
3. Install `pysl` for development (preferably in a separate virtual environment) running
   ```shell script
   pip install -r requirements.txt
   ```


## Running the test suite

```shell script
pytest
```
